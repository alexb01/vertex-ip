<!---
const credentials = [
  'Former USPTO Patent Examiner',
  'Registered Patent Practitioner (Reg. No. 81,681)',
  'MIT Alumnus',
];
---!>

<section id="hero" class="py-20 md:py-28 bg-[var(--color-bg)]">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
      Patent Drafting and Prior Art Searching
    </h1>

    <p class="text-lg md:text-xl text-[var(--color-secondary)] mb-8 max-w-3xl mx-auto leading-relaxed">
      Vertex IP is a patent drafting and services company owned and operated by Alexander Bost (USPTO Reg. No. 81,681), a registered patent practitioner, former patent examiner, and MIT alumnus.
    </p>

    <!---
    <div class="flex flex-wrap justify-center gap-3 mb-10">
      {credentials.map((credential) => (
        <span class="px-4 py-2 text-sm bg-[var(--color-bg-subtle)] text-[var(--color-secondary)] rounded-full border border-gray-200">
          {credential}
        </span>
      ))}
    </div>
    ---!>

    <a
      href="#contact"
      class="inline-block px-8 py-3 bg-[var(--color-accent)] text-white font-medium rounded-md hover:opacity-90 transition-opacity no-underline"
    >
      Get in Touch
    </a>
  </div>
</section>
